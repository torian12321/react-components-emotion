import * as mixins from './mixins';
import * as colors from './colors';
import * as animations from './animations';
import * as border from './borders';

export default colors;
export {mixins, colors, animations, border}